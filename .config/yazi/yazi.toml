[opener]
imv_image = [
  { run = 'imv "$0"', desc = "imv Image Viewer", block = false, for = "unix" },
]

mpv_video = [
  { run = 'mpv "$0"', desc = "mpv Video Player", block = false, for = "unix" },
]

zathura_pdf = [
  { run = 'zathura "$0"', desc = "Zathura PDF Reader", block = false, for = "unix" },
]

nvim_code = [
  { run = 'nvim "$0"', desc = "Neovim Code Editor", block = true, for = "unix" },
]

edit = [ { run = 'nvim "$@"', block = true, desc = "Neovim" } ]

[open]
prepend_rules = [
  # Images → imv
  { mime = "image/*", use = "imv_image" },

  # Videos → mpv
  { mime = "video/*", use = "mpv_video" },

  # PDFs → zathura
  { mime = "application/pdf", use = "zathura_pdf" },

  # Code files → nvim
  { mime = "text/*", use = "nvim_code" },
]

[mgr]
show_hidden = true

[theme]
use = "catppuccin-mocha"

[icons]
use = "catppuccin"

